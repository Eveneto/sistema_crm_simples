name: Deploy to Staging

# TEMPORARIAMENTE DESABILITADO - Vercel secrets não configurados
# Para habilitar: Configure VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID
# Ver docs/GITHUB_SECRETS_SETUP.md para instruções

on:
  # Desabilitado - remova comentário para ativar
  # push:
  #   branches: [sprint-2, sprint-3, sprint-4, sprint-5, sprint-6]
  workflow_dispatch: # Mantém opção de deploy manual

jobs:
  deploy:
    name: Deploy to Vercel Staging
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run tests
        run: npm test -- --passWithNoTests
        
      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./
          
      - name: Comment deployment URL
        if: github.event_name == 'push'
        run: |
          echo "🚀 Deployed to staging successfully!"
          echo "Branch: ${{ github.ref_name }}"
